s=("Hello world")
def lastword(s):    #Remove white space #s=arguments. count the words
        count=0
        last_word=0 #forloop or if statement(Have to know the length and run more times)
        for i in s: #Read everything in s
            if i==" ":
                count=0
            else:
                count==count+1
                length_last=count
        return length_last;
print (lastword(s))
 #            O(n) time space O(1)
            # #BigOn  as no creating of new variables/ reusing the Variables






